# Production frontend — points to your Render backend
# This value is OVERRIDDEN by the VITE_API_BASE_URL environment variable
# set in your Vercel project dashboard (Settings → Environment Variables).
# Update it there to: https://<your-render-service-name>.onrender.com/api
VITE_API_BASE_URL=https://advtaskmanager.onrender.com/api

